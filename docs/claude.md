# Claude Instructions

The project-level Claude instructions live in `.claude/CLAUDE.md` at the repository root.
That file is not committed to version control — it contains machine-local settings and is
listed in `.gitignore`. Read it directly in a local checkout.
